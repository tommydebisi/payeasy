[workspace]
members = [
    "contracts/rent-escrow",
]
resolver = "2"

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
panic = "abort"
# Optimize for size
codegen-units = 1
lto = true

[profile.release-with-logs]
inherits = "release"
debug = true
